{
  "name": "otto-eventbridge-lambda",
  "version": "1.0.0",
  "description": "Lambda function for processing EventBridge events from CodeBuild",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r lambda-deployment.zip dist/ node_modules/",
    "test": "node dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.109",
    "@types/node": "^18.11.9",
    "typescript": "^4.9.3"
  }
}